// Home page for the chatbot flow builder application. Renders the FlowBuilder component.

import FlowBuilder from "@/components/flowBuilder/FlowBuilder";

export default function Home() {
	return <FlowBuilder />;
}
